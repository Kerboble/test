{
  "name": "studysphere",
  "version": "1.0.0",
  "description": "All in one LMS application to mimic an in-person classroom as close as possible.",
  "main": "index.js",
  "scripts": {
    "install": "cd client && npm install && cd .. && cd server && npm install",
    "build": "cd client && npm run dev && cd ../server && npm run build",
    "start": "cd server && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mui/material": "^5.15.15",
    "@nivo/bar": "^0.85.1",
    "concurrently": "^8.2.2",
    "recharts": "^2.12.5",
    "vite": "^5.2.0"
  }
}
